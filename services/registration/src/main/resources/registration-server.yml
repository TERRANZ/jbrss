# Configure this Discovery Server
eureka:
  instance:
    hostname: xn--80aafhfrpg0adapheyc1nya.xn--p1ai
    preferIpAddress: true
  client:  # Not a client, don't register with yourself
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

server:
  port: 1111   # HTTP (Tomcat) port

zuul:
  routes:
    acc:
      path: /acc/**
      url: http://xn--80aafhfrpg0adapheyc1nya.xn--p1ai:2222
    rss:
      path: /rss/**
      url: http://xn--80aafhfrpg0adapheyc1nya.xn--p1ai:2224