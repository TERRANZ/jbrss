# Configure this Discovery Server
eureka:
  instance:
    hostname: localhost
    preferIpAddress: true
  client:  # Not a client, don't register with yourself
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://192.168.0.101:18080/eureka/

server:
  port: 18080   # HTTP (Tomcat) port