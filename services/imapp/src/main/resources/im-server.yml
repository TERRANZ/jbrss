# Spring properties
spring:
  application:
     name: im-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 2223   # HTTP (Tomcat) port


jabber:
  server: a
  port: 5555
  user: a
  pass: a
  enable: a
telegram:
  botname: jbrssbot
  token: b
  enable: c